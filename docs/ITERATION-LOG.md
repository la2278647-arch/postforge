# PostForge 迭代日志（ITERATION LOG）

> 本文件记录每一轮迭代的变更内容与优化目标，是全程迭代（目标 1000 轮）的决策留痕。
> 格式：`## 第 N 轮（YYYY-MM-DD）` → 优化目标 / 变更内容 / 验证结果。

---

## 第 1 轮（2026-09-15）—— 基线同步与推广素材升级

**优化目标**：把项目基线（v0.6.1）与全部推广素材对齐，补齐阶段一调研成果存档，建立迭代日志体系。

**变更内容**：

1. **调研存档**：新增 `docs/research/market-research.md`（阶段一成果）——完成市场方向调研（MCP 生态/排版工具刚需），记录候选方向对比、选择理由、目标用户画像、数据来源。
2. **推广文案全面升级至 v0.6.1**（此前停留在 v0.2.0，功能数据严重过时）：
   - 掘金 `docs/promotion/juejin.md`：测试 36 → 78、功能列表补齐（15 平台/10 主题/11 模板、serve/new/demo/batch --json 等）
   - V2EX `docs/promotion/v2ex.md`：平台 11 → 15、测试 36 → 78、主题 6 → 10
   - 知乎 `docs/promotion/zhihu.md`：主题 3 → 10、补 serve/check 增强
   - Twitter `docs/promotion/twitter.md`：补 15 平台/10 主题/在线 Demo
   - Hacker News `docs/promotion/hackernews.md`：主题 6 → 10、测试 36 → 78
   - Reddit / 微博 `docs/promotion/reddit.md`、`weibo.md`：同步最新数据
3. **发布指引更新**：`docs/promotion/README.md` 版本标注 v0.2.0 → v0.6.1，补充发布待办。
4. **模板库补全**：`examples/templates/moving-checklist.md`（搬家清单模板）纳入版本库（此前未跟踪）。
5. **CHANGELOG**：补记本轮文档/模板迭代。

**验证结果**：`npm test` 78 用例全绿；`postforge list` / `new list` 输出 15 平台、10 主题、11 模板；已推送 GitHub `main`。

---

## 第 2 轮（2026-09-15）—— 平台扩展至 16（+Hashnode）

**优化目标**：继续扩大平台覆盖面，新增国际开发者博客平台 Hashnode。

**变更内容**：

1. **平台 +1**：`src/platforms.js` 新增 `hashnode`（国际开发者博客，内联样式富文本）→ 共 16 平台。
2. **文档同步**：README 中英（平台表 16 行、徽章 `platforms-16`、特性数）；doctor 断言 15 → 16。
3. **版本**：v0.6.2（待发布）。

**验证结果**：`npm test` 78 用例全绿；`build -p hashnode` 渲染正常；已推送 GitHub `main`。

---

## 第 3 轮（2026-09-15）—— 模板库 12 篇（+学习计划）

**优化目标**：继续扩充开箱即用模板，新增学习计划场景。

**变更内容**：

1. **模板 +1**：`examples/templates/study-plan.md`（学习计划：目标拆解卡 / 阶段规划 / 资源清单 / 每周复盘表 / 防放弃提醒）→ 共 12 篇。
2. **文档同步**：README 中英模板表 11 → 12。
3. **版本**：v0.6.3（待发布）。

**验证结果**：`npm test` 78 用例全绿；`check` / `build` study-plan 模板正常；已推送 GitHub `main`。

---

## 第 4 轮（2026-09-15）—— 代码卡片语法（:::code）

**优化目标**：新增带标题的代码卡片语法，满足技术文「代码卡片」排版需求。

**变更内容**：

1. **语法 +1**：`src/cards.js` 新增 `:::code 标题` + \`\`\` 代码 \`\`\` + `:::`（需闭合）——渲染为「标题栏 + 高亮代码块」卡片，代码高亮自动生效。
2. **兼容**：`check` 将 code 视为需闭合卡片（未闭合报错）；小红书模式输出「【代码：标题】+ 代码文本」。
3. **文档**：README 模板文档补 `:::code`（8 种语法）；测试 78 → 82（code 4 用例）。
4. **版本**：v0.6.4（待发布）。

**验证结果**：`npm test` 82 用例全绿；CLI 实测标题栏/代码块/高亮正常；已推送 GitHub `main`。

---

## 第 5 轮（2026-09-15）—— 主题扩展至 11（+mint 薄荷绿）

**优化目标**：继续丰富主题覆盖，新增生活 / 健康类清新浅色主题。

**变更内容**：

1. **主题 +1**：`src/themes.js` 新增 `mint`（薄荷绿清新浅色，适合生活/健康/自然类内容）→ 共 11 套主题。
2. **测试**：主题完整性 / 互异性列表扩展至 11。
3. **文档**：README 中英主题表 10 → 11。
4. **版本**：v0.6.5（待发布）。

**验证结果**：`npm test` 82 用例全绿；`build --theme mint` 渲染正常；已推送 GitHub `main`。

---
